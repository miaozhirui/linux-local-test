1.数据库的命名规范和其他编程语言基本一样
  create database if not exists class charset gbk;
  show create database class;
2.修改数据库
 alter database class character set = utf-8;
3.删除数据库
  drop database if exists class
3.数据类型和创建数据表
  mysql默认端口是3306;
  mysql中的超级用户叫 root;
  创建数据库 create database
  修改数据库 alter database
  删除数据库 drop database
4.数据类型（列在存储过程参数，表达式，局部变量的数据特征，决定了存储格式和代表了不同的数据特征）
5. 整型
    tinyint smallint mediumint int bigint
6.浮点型
    float  double
7.日期时间型
    year time date datetime timestamp
8.字符型
    char(定长) varchar(变长) tinytext text  mediumtext 
    longtext enum('value1', 'value2') set('value1', 'value2')
9.数据表（数据表或称表是数据库最重要的组成部分之一，是其他对象的基础）
10.use 打开数据库
    use +数据库名称
    select database();显示当前打开的数据库
    creta tabale [if not exists] table_name(

    )
  11.创建数据表
    create table tb1(
    usename varchar(20),
    age tinyint unsigned,
    salary float(8,2) unsigned
    );    



  













